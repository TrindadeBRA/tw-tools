import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Validadores | TW Tools",
    description: "Valide dados com nossos validadores de dados. Verifique se um CPF, CNPJ, RG, CNH, CEP, senha ou outro dado é válido.",
};


export default function Validadores() {
    return <div>Validadores</div>
}
